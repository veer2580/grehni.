# ==============================================================================
# Cloudflare Workers Static Assets - Redirects
# NOTE: SPA fallback is handled by wrangler.toml (not_found_handling = "single-page-application")
# Only use this file for explicit path redirects, not SPA catch-all.
# ==============================================================================

# Redirect www to non-www (canonical domain)
https://www.grehni.com/*  https://grehni.com/:splat  301
