# ==============================================================================
# Cloudflare Workers Static Assets - Redirects
# NOTE: Only relative URLs are supported in Workers _redirects.
# SPA fallback is handled by wrangler.toml (not_found_handling = "single-page-application")
# www -> non-www redirect must be set in Cloudflare Dashboard > Rules > Redirect Rules
# ==============================================================================
